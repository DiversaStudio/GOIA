docker-compose up -d

# Run migrations
make migrate

# Start backend
docker-compose up -d backend

# Start frontend  
docker-compose up -d frontend

# Access services:
# Frontend: http://localhost:3000
# API: http://localhost:8000/docs
# DB (local only): localhost:5432
